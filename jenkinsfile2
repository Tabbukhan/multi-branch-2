pipeline {
    agent {
        docker { image 'node:20.0.7,milestone' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
